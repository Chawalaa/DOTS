[theme]
# Pastel accent (Soft blue)
primaryColor = "#8FB9FF"

# App background (very light warm-white)
backgroundColor = "#FFFDF8"

# Sidebar + containers background (very light pastel)
secondaryBackgroundColor = "#F3F7FF"

# Text
textColor = "#2B2B2B"

# Font
font = "sans serif"
